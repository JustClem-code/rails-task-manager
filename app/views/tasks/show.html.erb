<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<p><%= @task.completed %></p>
<%# <div>
  <input type="checkbox" id="scales" name="scales" value="" />
  <label for="scales">Scales</label>
</div> %>
<%= link_to "Back to list", tasks_path %>
<%= link_to "Edit", edit_task_path %>
<%= link_to "Delete",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
